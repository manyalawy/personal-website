---
phase: 02-core-content--sections
plan: 04
type: execute
wave: 3
depends_on: ["02-01", "02-02", "02-03"]
files_modified:
  - src/app/page.tsx
autonomous: false

must_haves:
  truths:
    - "All 7 sections render on the page in correct order"
    - "Navigation links scroll to correct sections"
    - "Page builds successfully with static export"
    - "Content is readable on mobile and desktop"
    - "Alternating section backgrounds create visual separation"
  artifacts:
    - path: "src/app/page.tsx"
      provides: "Full page assembly with all section components"
      contains: "Hero"
  key_links:
    - from: "src/app/page.tsx"
      to: "src/components/sections/Hero.tsx"
      via: "import and render"
      pattern: "import.*Hero"
    - from: "src/app/page.tsx"
      to: "src/components/sections/Projects.tsx"
      via: "import and render"
      pattern: "import.*Projects"
    - from: "src/app/page.tsx"
      to: "src/components/sections/Contact.tsx"
      via: "import and render"
      pattern: "import.*Contact"
---

<objective>
Wire all section components into page.tsx and verify the complete portfolio renders.

Purpose: Integrates all independently-built sections into a single cohesive page. This is the moment everything comes together.
Output: Complete portfolio page with all 7 sections rendering in order.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-core-content--sections/02-CONTEXT.md
@src/app/page.tsx
@src/components/sections/Hero.tsx
@src/components/sections/Skills.tsx
@src/components/sections/Experience.tsx
@src/components/sections/Education.tsx
@src/components/sections/Projects.tsx
@src/components/sections/About.tsx
@src/components/sections/Contact.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire all sections into page.tsx</name>
  <files>src/app/page.tsx</files>
  <action>
Replace the placeholder sections in page.tsx with the actual section components.

Import all 7 section components and render them in this order:
1. Hero (full viewport, bg-surface)
2. About (bg-surface-secondary)
3. Skills (bg-surface)
4. Experience (bg-surface-secondary)
5. Education (bg-surface)
6. Projects (bg-surface-secondary -- or bg-surface, follow what looks best with alternating pattern)
7. Contact (final section)

Remove the old placeholder `<section>` elements entirely. The section components handle their own ids, backgrounds, and padding.

Keep the outer `<div className="min-h-screen bg-surface">` and `<main>` wrapper.

Run `npm run build` to verify static export succeeds with all components.
  </action>
  <verify>Run `npm run build` -- must succeed with no errors. Check that output includes the static page.</verify>
  <done>page.tsx imports and renders all 7 section components. Build succeeds.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete portfolio page with all 7 content sections: Hero, About, Skills, Experience, Education, Projects, and Contact. All sections use real component layouts with placeholder data.</what-built>
  <how-to-verify>
1. Run `npm run dev` and visit http://localhost:3000
2. Verify Hero section fills viewport with name, title, tagline, and CTA buttons
3. Scroll down -- verify all sections appear in order with alternating backgrounds
4. Click "View Projects" CTA in hero -- should scroll to Projects section
5. Click "Get in Touch" CTA -- should scroll to Contact section
6. Test navigation header links -- each should scroll to correct section
7. Toggle dark/light mode -- all sections should respect theme
8. Resize to mobile width -- verify all sections are responsive
9. Click contact links -- email should open mailto, GitHub/LinkedIn should open in new tab
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
- `npm run build` succeeds
- All 7 sections visible on page
- Navigation links work
- Theme toggle works across all sections
- Mobile responsive
</verification>

<success_criteria>
- Complete portfolio page renders all content sections
- User can navigate between sections
- Theme system works throughout
- Static export builds successfully
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-content--sections/02-04-SUMMARY.md`
</output>
