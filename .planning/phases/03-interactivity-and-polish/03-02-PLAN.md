---
phase: 03-interactivity-and-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/sections/Projects.tsx
  - src/components/sections/Skills.tsx
  - src/components/sections/Hero.tsx
  - src/components/sections/Contact.tsx
  - src/components/layout/Header.tsx
autonomous: true

must_haves:
  truths:
    - "User sees project cards scale up and shadow deepen on hover"
    - "User sees navigation links have visual feedback on hover"
    - "User sees CTA buttons have engaging hover effects"
    - "User sees skill badges respond to hover with subtle animation"
  artifacts:
    - path: "src/components/sections/Projects.tsx"
      provides: "Project cards with hover scale/shadow effects"
    - path: "src/components/sections/Skills.tsx"
      provides: "Skill badges with hover micro-interactions"
    - path: "src/components/sections/Hero.tsx"
      provides: "CTA buttons with hover effects"
    - path: "src/components/sections/Contact.tsx"
      provides: "Contact cards with hover effects"
    - path: "src/components/layout/Header.tsx"
      provides: "Navigation links with hover underline/color effects"
  key_links:
    - from: "All interactive components"
      to: "Tailwind CSS transition utilities"
      via: "className props"
      pattern: "transition-|hover:"
---

<objective>
Add playful hover effects to all interactive elements: project cards, navigation links, CTA buttons, skill badges, and contact cards. These micro-interactions create visual feedback that makes the portfolio feel responsive and professionally polished.

Purpose: Hover effects are a key part of the "playful & interactive" aesthetic. They demonstrate attention to detail and make the site feel alive and responsive to user input.

Output: Enhanced interactive components with smooth, GPU-accelerated hover animations.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/components/sections/Projects.tsx
@src/components/sections/Skills.tsx
@src/components/sections/Hero.tsx
@src/components/sections/Contact.tsx
@src/components/layout/Header.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add hover effects to project cards</name>
  <files>
    src/components/sections/Projects.tsx
  </files>
  <action>
    Enhance the project card div with hover effects:

    1. Add these Tailwind classes to the project card container:
       - `transition-all duration-300` - smooth transitions
       - `hover:scale-[1.02]` - subtle scale up on hover (2%)
       - `hover:shadow-lg` or `hover:shadow-xl` - deeper shadow
       - `hover:-translate-y-1` - slight lift effect

    2. Current card classes include `bg-surface-secondary rounded-xl overflow-hidden p-5 md:p-6 flex flex-col`

    3. Updated card should be:
       ```
       className="bg-surface-secondary rounded-xl overflow-hidden p-5 md:p-6 flex flex-col
                  transition-all duration-300 hover:scale-[1.02] hover:-translate-y-1 hover:shadow-lg"
       ```

    4. For the project links (Live Demo, Source Code):
       - They already have `hover:opacity-80` - this is fine
       - Optionally add `hover:translate-x-1` for a subtle slide effect

    Only use GPU-accelerated properties (transform, opacity, shadow changes via filter).
  </action>
  <verify>
    - `npm run dev` and hover over project cards
    - Cards should smoothly scale up and lift with shadow
    - Hover state should be smooth (no jank)
  </verify>
  <done>
    Project cards have scale, lift, and shadow hover effects (ANIM-03)
  </done>
</task>

<task type="auto">
  <name>Task 2: Add hover effects to navigation links and skill badges</name>
  <files>
    src/components/layout/Header.tsx
    src/components/sections/Skills.tsx
  </files>
  <action>
    **Header.tsx - Navigation link hover effects:**

    1. Desktop nav links currently have `text-text-secondary hover:text-text-primary transition-colors`
    2. Add underline animation effect. Updated classes:
       ```
       className="text-text-secondary hover:text-accent transition-colors relative
                  after:absolute after:bottom-0 after:left-0 after:h-0.5 after:w-0 after:bg-accent
                  after:transition-all after:duration-300 hover:after:w-full"
       ```

    3. Mobile nav links already have good hover states - ensure they work smoothly

    **Skills.tsx - Skill badge micro-interactions:**

    1. Skill badge currently has `hover:border-accent transition-colors`
    2. Enhance with scale and icon animation:
       ```
       className="flex flex-col items-center justify-center p-3 md:p-4 bg-surface-secondary
                  border border-border rounded-lg transition-all duration-300
                  hover:border-accent hover:scale-105 hover:-translate-y-1"
       ```

    3. For the icon container (w-12 h-12 div), add:
       ```
       className="w-12 h-12 md:w-14 md:h-14 rounded-full bg-accent/10 flex items-center justify-center mb-2
                  transition-transform duration-300 group-hover:scale-110"
       ```
       AND add `group` class to the parent badge div

    This creates a micro-interaction where hovering the badge makes the icon "pop" slightly.
  </action>
  <verify>
    - `npm run dev`
    - Hover over nav links - should see underline animate from left to right
    - Hover over skill badges - should see badge lift and icon scale up
  </verify>
  <done>
    Navigation links have animated underline effect (ANIM-04), skill badges have micro-interactions (ANIM-06)
  </done>
</task>

<task type="auto">
  <name>Task 3: Add hover effects to buttons and CTAs</name>
  <files>
    src/components/sections/Hero.tsx
    src/components/sections/Contact.tsx
  </files>
  <action>
    **Hero.tsx - CTA button hover effects:**

    1. Primary CTA (View Projects) currently has `hover:opacity-90`
    2. Enhance with scale and glow effect:
       ```
       className="inline-flex items-center justify-center px-8 py-3 md:px-10 md:py-4
                  text-base md:text-lg font-semibold text-white bg-accent rounded-lg
                  transition-all duration-300 hover:scale-105 hover:shadow-lg hover:shadow-accent/25
                  min-w-[200px]"
       ```

    3. Secondary CTA (Get in Touch) currently has border hover:
       ```
       className="inline-flex items-center justify-center px-8 py-3 md:px-10 md:py-4
                  text-base md:text-lg font-semibold text-text-primary border-2 border-border rounded-lg
                  transition-all duration-300 hover:border-accent hover:text-accent hover:scale-105
                  min-w-[200px]"
       ```

    **Contact.tsx - Contact card hover effects:**

    1. Contact cards already have `hover:border-accent border-2 border-transparent`
    2. Enhance with scale and lift:
       ```
       className="flex flex-col items-center justify-center bg-surface-secondary rounded-lg p-4 md:p-6
                  min-w-[160px] border-2 border-transparent transition-all duration-300
                  hover:border-accent hover:scale-105 hover:-translate-y-1 hover:shadow-md"
       ```

    3. For the emoji icon div, add group hover effect:
       - Add `group` to parent card
       - Add `transition-transform duration-300 group-hover:scale-110` to the emoji div
  </action>
  <verify>
    - `npm run dev`
    - Hover over Hero CTAs - primary should scale with glow, secondary should scale with border
    - Hover over Contact cards - should lift with border accent and icon scale
    - All transitions should be smooth (300ms duration)
  </verify>
  <done>
    CTA buttons have scale and glow hover effects (ANIM-05), Contact cards enhanced
  </done>
</task>

</tasks>

<verification>
1. Run `npm run build` - should complete without errors
2. Run `npm run dev` and test all hover effects:
   - Project cards: scale up, lift, shadow
   - Navigation links: animated underline
   - Skill badges: lift and icon pop
   - Hero CTAs: scale with glow/border
   - Contact cards: lift with border and icon scale
3. Mobile test:
   - On touch devices, tap states should work appropriately
   - Hover effects should not persist after touch ends
4. Performance:
   - All transitions should be smooth (60fps)
   - Only transform/opacity/shadow properties animated (GPU-accelerated)
</verification>

<success_criteria>
- ANIM-03: ✅ Hover effects on project cards (scale, shadow, transform)
- ANIM-04: ✅ Hover effects on navigation links (animated underline)
- ANIM-05: ✅ Hover effects on buttons and CTAs (scale, glow)
- ANIM-06: ✅ Micro-interactions on skill badges (lift, icon scale)
- ANIM-07: ✅ All animations use GPU-accelerated properties
</success_criteria>

<output>
After completion, create `.planning/phases/03-interactivity-and-polish/03-02-SUMMARY.md`
</output>
