---
phase: 03-interactivity-and-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/motion/ScrollReveal.tsx
  - src/components/sections/Hero.tsx
  - src/components/sections/About.tsx
  - src/components/sections/Skills.tsx
  - src/components/sections/Experience.tsx
  - src/components/sections/Education.tsx
  - src/components/sections/Projects.tsx
  - src/components/sections/Contact.tsx
autonomous: true

must_haves:
  truths:
    - "User sees sections animate into view when scrolling down the page"
    - "Animations only trigger once as sections enter viewport"
    - "User with prefers-reduced-motion sees no animations"
    - "Animations are smooth with no jank on mobile devices"
  artifacts:
    - path: "src/components/motion/ScrollReveal.tsx"
      provides: "Reusable scroll-triggered animation wrapper using Framer Motion"
      exports: ["ScrollReveal"]
    - path: "src/components/sections/*.tsx"
      provides: "All 7 sections wrapped with ScrollReveal for entry animations"
  key_links:
    - from: "src/components/sections/*.tsx"
      to: "src/components/motion/ScrollReveal.tsx"
      via: "import and wrapper component"
      pattern: "import.*ScrollReveal"
---

<objective>
Create the scroll-triggered animation system using Framer Motion's built-in viewport detection (which uses Intersection Observer internally). Wrap all 7 portfolio sections with smooth fade-in-up reveal animations that respect user motion preferences.

Purpose: This establishes the core animation infrastructure that makes the portfolio feel polished and professional. Scroll-triggered reveals create visual interest and guide attention as visitors explore the site.

Output: A reusable ScrollReveal component and all sections enhanced with entry animations.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/components/sections/Hero.tsx
@src/components/sections/About.tsx
@src/components/sections/Skills.tsx
@src/components/sections/Experience.tsx
@src/components/sections/Education.tsx
@src/components/sections/Projects.tsx
@src/components/sections/Contact.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Framer Motion and create ScrollReveal component</name>
  <files>
    package.json
    src/components/motion/ScrollReveal.tsx
  </files>
  <action>
    1. Install framer-motion if not already installed: `npm install framer-motion`

    2. Create `src/components/motion/ScrollReveal.tsx` with:
       - "use client" directive (required for Framer Motion)
       - Import `motion` and `useReducedMotion` from framer-motion
       - Props: children, className (optional), delay (optional, default 0)
       - Use motion.div with `whileInView` for viewport-triggered animation
       - Animation: fade-in (opacity 0→1) + slide-up (y: 20→0)
       - Use `once: true` in viewport options to only animate once
       - Duration: 0.5s with ease-out easing
       - If `useReducedMotion()` returns true, render children without animation wrapper
       - Use only GPU-accelerated properties: transform (translateY) and opacity

    Example structure:
    ```tsx
    "use client";
    import { motion, useReducedMotion } from "framer-motion";

    interface ScrollRevealProps {
      children: React.ReactNode;
      className?: string;
      delay?: number;
    }

    export function ScrollReveal({ children, className, delay = 0 }: ScrollRevealProps) {
      const shouldReduceMotion = useReducedMotion();

      if (shouldReduceMotion) {
        return <div className={className}>{children}</div>;
      }

      return (
        <motion.div
          className={className}
          initial={{ opacity: 0, y: 20 }}
          whileInView={{ opacity: 1, y: 0 }}
          viewport={{ once: true, margin: "-100px" }}
          transition={{ duration: 0.5, delay, ease: "easeOut" }}
        >
          {children}
        </motion.div>
      );
    }
    ```
  </action>
  <verify>
    - File exists at src/components/motion/ScrollReveal.tsx
    - `npm run build` succeeds without errors
  </verify>
  <done>
    ScrollReveal component created with viewport detection, once-only animation, and reduced-motion support
  </done>
</task>

<task type="auto">
  <name>Task 2: Add scroll reveal animations to all sections</name>
  <files>
    src/components/sections/Hero.tsx
    src/components/sections/About.tsx
    src/components/sections/Skills.tsx
    src/components/sections/Experience.tsx
    src/components/sections/Education.tsx
    src/components/sections/Projects.tsx
    src/components/sections/Contact.tsx
  </files>
  <action>
    For each of the 7 section components:

    1. Add "use client" directive at the top (required for Framer Motion animations)
    2. Import ScrollReveal: `import { ScrollReveal } from "@/components/motion/ScrollReveal";`
    3. Wrap the main content container (inside the section, after the background) with ScrollReveal

    **Hero.tsx:**
    - Wrap the hero content div (the one with className containing "text-center max-w-5xl")
    - Keep section itself unwrapped (it's full viewport height, always visible)

    **About.tsx:**
    - Wrap the max-w-4xl container with ScrollReveal

    **Skills.tsx:**
    - Wrap the max-w-6xl container with ScrollReveal
    - Optionally add staggered delays to skill category groups (delay=0, 0.1, 0.2)

    **Experience.tsx:**
    - Wrap the max-w-4xl container with ScrollReveal

    **Education.tsx:**
    - Wrap the max-w-4xl container with ScrollReveal

    **Projects.tsx:**
    - Wrap the max-w-6xl container with ScrollReveal

    **Contact.tsx:**
    - Wrap the max-w-4xl container with ScrollReveal

    IMPORTANT: Do NOT wrap the outer `<section>` element - keep section IDs and scroll-mt-16 intact for navigation. Only wrap the inner content container.
  </action>
  <verify>
    - `npm run build` succeeds
    - `npm run dev` and scroll through page - sections should fade in as they enter viewport
    - Enable "Reduce motion" in OS accessibility settings and verify animations are disabled
  </verify>
  <done>
    All 7 sections have scroll-triggered reveal animations that respect prefers-reduced-motion
  </done>
</task>

</tasks>

<verification>
1. Run `npm run build` - should complete without errors
2. Run `npm run dev` and test scroll animations:
   - Open Chrome DevTools, set mobile viewport (iPhone 12)
   - Scroll down slowly - each section should fade in smoothly
   - Animations should only play once (scroll back up and down - no re-animation)
3. Test reduced motion:
   - macOS: System Preferences → Accessibility → Display → Reduce motion
   - Windows: Settings → Ease of Access → Display → Show animations
   - With reduced motion ON, sections should appear without animation
4. Performance check:
   - Open DevTools Performance tab
   - Record while scrolling
   - Verify no long tasks or frame drops (should maintain 60fps)
</verification>

<success_criteria>
- ANIM-01: ✅ Scroll-triggered reveal animations on section entry
- ANIM-02: ✅ Framer Motion uses Intersection Observer internally (no scroll listeners)
- ANIM-07: ✅ Only GPU-accelerated properties used (transform, opacity)
- ANIM-08: ✅ Animations respect prefers-reduced-motion
- ANIM-10: ✅ Animations tested and smooth on mobile viewport
</success_criteria>

<output>
After completion, create `.planning/phases/03-interactivity-and-polish/03-01-SUMMARY.md`
</output>
