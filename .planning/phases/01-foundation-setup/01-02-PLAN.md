---
phase: 01-foundation-setup
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - src/components/layout/Header.tsx
  - src/app/page.tsx
  - src/app/layout.tsx
  - src/app/globals.css
autonomous: false

must_haves:
  truths:
    - "Navigation header sticks to top when scrolling"
    - "Clicking nav links smoothly scrolls to corresponding sections"
    - "Navigation collapses to mobile menu on small screens"
    - "Layout is responsive across mobile (320px+), tablet (768px+), desktop (1024px+)"
    - "Theme toggle is accessible from the navigation header"
    - "Core Web Vitals meet targets: FCP <1.8s, CLS <0.1"
  artifacts:
    - path: "src/components/layout/Header.tsx"
      provides: "Sticky navigation with nav links, mobile menu, and theme toggle"
      contains: "sticky top-0"
    - path: "src/app/page.tsx"
      provides: "Page with section containers using responsive Tailwind classes"
      contains: "md:"
  key_links:
    - from: "src/components/layout/Header.tsx"
      to: "src/components/theme/ThemeToggle.tsx"
      via: "import and render in nav"
      pattern: "ThemeToggle"
    - from: "src/components/layout/Header.tsx"
      to: "src/app/page.tsx"
      via: "anchor hrefs matching section IDs"
      pattern: '#(about|skills|experience|projects|contact)'
    - from: "src/app/layout.tsx"
      to: "src/components/layout/Header.tsx"
      via: "import and render above children"
      pattern: "Header"
---

<objective>
Build sticky navigation header with responsive mobile menu and finalize responsive layout structure.

Purpose: Completes the foundation requirements for navigation (FOUND-07), responsive design (FOUND-01, FOUND-02), Core Web Vitals baseline (FOUND-06), and ensures theme toggle is accessible from any page (FOUND-04).
Output: Sticky header with nav links, mobile hamburger menu, theme toggle integrated, and responsive section layout.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-setup/01-RESEARCH.md
@.planning/phases/01-foundation-setup/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create sticky Header with navigation and mobile menu</name>
  <files>
    src/components/layout/Header.tsx
    src/app/layout.tsx
    src/app/page.tsx
    src/app/globals.css
  </files>
  <action>
    1. Create src/components/layout/Header.tsx:
       - Sticky header: `sticky top-0 z-50 backdrop-blur-sm` with semi-transparent bg (bg-white/80 dark:bg-gray-900/80)
       - Bottom border for visual separation (border-b border-gray-200 dark:border-gray-800)
       - Nav container: `max-w-6xl mx-auto px-4 sm:px-6 lg:px-8 flex items-center justify-between h-16`
       - Logo/name link on left: `<a href="#">Portfolio</a>` with font-bold
       - Desktop nav links (hidden on mobile, flex on md+): About, Skills, Experience, Projects, Contact
         - Each link: `<a href="#section-id">` with hover color transitions
       - Mobile hamburger button (visible on mobile, hidden on md+):
         - Use a "use client" directive on the Header component
         - useState for mobile menu open/close
         - Hamburger icon (three lines via spans or SVG)
         - When open, show dropdown menu with nav links
         - Close menu when a link is clicked
       - ThemeToggle component rendered in the nav bar (visible on all screen sizes)

    2. Update src/app/layout.tsx:
       - Import Header component
       - Render Header above {children} inside ThemeProvider
       - Ensure body has responsive padding: no horizontal padding (Header handles its own)

    3. Update src/app/page.tsx:
       - Structure each placeholder section with responsive containers:
         - `<section id="hero" className="min-h-screen flex items-center justify-center px-4 md:px-8 lg:px-16">`
         - Similar for about, skills, experience, projects, contact
         - Each section gets a visible heading (h2) with the section name
         - Use responsive text sizes: `text-3xl md:text-4xl lg:text-5xl`
         - Alternate subtle background colors between sections for visual distinction
       - Hero section should be full viewport height, others can be shorter (min-h-[70vh])
       - Add `scroll-mt-16` to each section to offset for sticky header height

    4. If needed, add any utility classes to globals.css (e.g., scroll-margin-top utility if not built into Tailwind v4).
  </action>
  <verify>
    `npm run build` succeeds.
    Run `npm run dev` and verify:
    - Header stays fixed at top when scrolling
    - Desktop: nav links visible, clicking scrolls smoothly to section
    - Resize to mobile width: nav links hidden, hamburger visible
    - Click hamburger: menu opens with links, clicking link scrolls and closes menu
    - Theme toggle visible and functional in header on all screen sizes
    - Sections have responsive padding and text sizes
    - `scroll-mt-16` prevents header from covering section headings
  </verify>
  <done>
    Sticky header with desktop nav links and mobile hamburger menu. Smooth scroll to sections works. Theme toggle accessible from header on all screen sizes. Responsive layout with mobile-first breakpoints applied to all sections.
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify Core Web Vitals meet FOUND-06 targets</name>
  <files></files>
  <action>
    Run Lighthouse CI against the built site to verify Core Web Vitals baseline:

    1. Build the site: `npm run build`
    2. Install Lighthouse CLI if not present: `npx lighthouse --version` or use `npx @lhci/cli`
    3. Serve the static `out/` directory locally: `npx serve out -p 3001 &`
    4. Run Lighthouse in headless mode:
       `npx lighthouse http://localhost:3001 --output=json --output-path=./lighthouse-report.json --chrome-flags="--headless --no-sandbox" --only-categories=performance`
    5. Parse the JSON report and check:
       - First Contentful Paint (FCP) < 1.8s (1800ms)
       - Cumulative Layout Shift (CLS) < 0.1
    6. Log the actual FCP and CLS values.
    7. Kill the serve process.
    8. Remove lighthouse-report.json (don't commit it).

    If metrics fail, investigate: check for render-blocking resources, large images, layout shifts from theme toggle or mobile menu, and fix accordingly.
  </action>
  <verify>
    Lighthouse report shows FCP < 1.8s and CLS < 0.1. Log exact values.
  </verify>
  <done>Core Web Vitals verified: FCP under 1.8s and CLS under 0.1 on the static build.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete Phase 1 foundation: Next.js 16 app with Tailwind v4, dark/light theme toggle, sticky navigation with smooth scroll, responsive layout across mobile/tablet/desktop, mobile hamburger menu, and verified Core Web Vitals (FCP &lt;1.8s, CLS &lt;0.1).</what-built>
  <how-to-verify>
    1. Run `npm run dev` and open http://localhost:3000
    2. Verify theme toggle works: click it, see colors change smoothly, refresh page and confirm theme persists
    3. Scroll down: header should stick to top with blur effect
    4. Click nav links: page should smooth-scroll to matching sections
    5. Resize browser to mobile width (~375px): nav links collapse to hamburger menu
    6. Open hamburger menu, click a link: menu closes and scrolls to section
    7. Check responsive layout: sections should have appropriate padding at mobile/tablet/desktop widths
    8. Run `npm run build` and confirm it produces `out/` directory (static export)
    9. Confirm Core Web Vitals results from Task 2: FCP <1.8s and CLS <0.1
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
- `npm run build` produces static output in `out/`
- Header is sticky with backdrop blur
- Nav links scroll to correct sections with offset for header
- Mobile menu opens/closes correctly
- Theme toggle works from header on all screen sizes
- Responsive breakpoints applied (mobile-first)
- No console errors or hydration warnings
- Core Web Vitals: FCP <1.8s and CLS <0.1 (Lighthouse verified)
</verification>

<success_criteria>
- FOUND-01: Responsive across mobile (320px+), tablet (768px+), desktop (1024px+)
- FOUND-02: Mobile-first CSS with Tailwind breakpoint system (sm, md, lg)
- FOUND-04: Theme toggle accessible from navigation header (any page)
- FOUND-06: Core Web Vitals FCP <1.8s and CLS <0.1 (Lighthouse verified)
- FOUND-07: Sticky navigation header with smooth scroll to sections
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-setup/01-02-SUMMARY.md`
</output>
