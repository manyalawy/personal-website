---
phase: 01-foundation-setup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/layout.tsx
  - src/app/page.tsx
  - src/app/globals.css
  - src/components/theme/ThemeProvider.tsx
  - src/components/theme/ThemeToggle.tsx
  - next.config.ts
  - postcss.config.mjs
autonomous: true

must_haves:
  truths:
    - "Site renders in browser at localhost:3000 with no errors"
    - "User can toggle between dark and light themes"
    - "Theme preference persists after page refresh"
    - "No flash of wrong theme on page load"
  artifacts:
    - path: "src/app/globals.css"
      provides: "Tailwind v4 imports, dark mode custom variant, theme CSS variables, smooth scroll"
      contains: "@custom-variant dark"
    - path: "src/components/theme/ThemeProvider.tsx"
      provides: "Client component wrapping next-themes"
      contains: "use client"
    - path: "src/components/theme/ThemeToggle.tsx"
      provides: "Theme toggle button with mounted guard"
      contains: "useTheme"
    - path: "src/app/layout.tsx"
      provides: "Root layout with ThemeProvider wrapper and suppressHydrationWarning"
      contains: "suppressHydrationWarning"
    - path: "next.config.ts"
      provides: "Static export configuration"
      contains: 'output: "export"'
  key_links:
    - from: "src/app/layout.tsx"
      to: "src/components/theme/ThemeProvider.tsx"
      via: "import and wrapping children"
      pattern: "ThemeProvider"
    - from: "src/components/theme/ThemeToggle.tsx"
      to: "next-themes"
      via: "useTheme hook"
      pattern: "useTheme"
    - from: "src/app/globals.css"
      to: "tailwindcss"
      via: "@import directive"
      pattern: '@import "tailwindcss"'
---

<objective>
Scaffold Next.js 16 project with Tailwind CSS v4 and dark/light theme system using next-themes.

Purpose: Establish the production-ready foundation that all subsequent phases build on. Without this, nothing else can be built.
Output: Working Next.js app with theme toggle, CSS variables for both themes, static export config, and smooth scroll.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-setup/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Next.js 16 project and install dependencies</name>
  <files>
    package.json
    next.config.ts
    postcss.config.mjs
    tsconfig.json
  </files>
  <action>
    Run `npx create-next-app@latest . --typescript --eslint --app --src-dir --import-alias "@/*" --tailwind` to scaffold the project in the current directory. If it fails because files exist, use `--yes` or handle accordingly.

    Then install next-themes: `npm install next-themes`

    Configure static export in next.config.ts:
    ```typescript
    import type { NextConfig } from "next";
    const nextConfig: NextConfig = {
      output: "export",
    };
    export default nextConfig;
    ```

    Verify postcss.config.mjs uses `@tailwindcss/postcss` (Tailwind v4 pattern). If create-next-app scaffolded with v3 config, convert to v4:
    - Remove tailwind.config.js/ts if created
    - Ensure postcss.config.mjs has `@tailwindcss/postcss` plugin

    Run `npm run dev` briefly to confirm the app starts without errors, then stop the server.
  </action>
  <verify>
    `npm run build` completes without errors. `ls out/` shows generated static files.
  </verify>
  <done>Next.js 16 project scaffolded with TypeScript, Tailwind v4, static export, and next-themes installed. Build succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: Configure theme system with Tailwind v4 dark mode and next-themes</name>
  <files>
    src/app/globals.css
    src/components/theme/ThemeProvider.tsx
    src/components/theme/ThemeToggle.tsx
    src/app/layout.tsx
    src/app/page.tsx
  </files>
  <action>
    1. Replace src/app/globals.css with Tailwind v4 setup:
       - `@import "tailwindcss";`
       - `@custom-variant dark (&:where(.dark, .dark *));`
       - Define theme CSS variables in `@theme inline {}` block: surface, surface-secondary, text-primary, text-secondary, accent, border colors
       - Add `.dark {}` block overriding those variables with dark palette
       - Add `* { transition: background-color 0.2s ease, color 0.2s ease, border-color 0.2s ease; }` for smooth theme transitions
       - Add `html { scroll-behavior: smooth; }` for anchor navigation
       - Use the exact color values from RESEARCH.md code examples

    2. Create src/components/theme/ThemeProvider.tsx:
       - "use client" directive
       - Import ThemeProvider from next-themes, re-export as named export
       - Props: `attribute="class"`, `defaultTheme="system"`, `enableSystem`
       - Accept and pass through children

    3. Create src/components/theme/ThemeToggle.tsx:
       - "use client" directive
       - Use `useTheme` hook from next-themes
       - Guard against hydration mismatch: `mounted` state with useEffect, return null if not mounted
       - Button toggles between dark/light
       - Include aria-label for accessibility
       - Style with Tailwind: padding, rounded, hover states for both themes

    4. Update src/app/layout.tsx:
       - Import and wrap children with ThemeProvider
       - Add `suppressHydrationWarning` to `<html>` tag
       - Set `lang="en"` on html
       - Add basic metadata export (title: "Portfolio", description)
       - Apply `font-sans` and theme-aware bg/text colors to body using CSS variable classes

    5. Update src/app/page.tsx:
       - Replace scaffolded content with a minimal page containing:
         - A heading "Portfolio" using theme-aware text color
         - The ThemeToggle component to verify it works
         - Placeholder section divs with IDs (hero, about, skills, experience, projects, contact) for future phases
         - Each section should have min-height and a visible label so scroll behavior can be tested
  </action>
  <verify>
    `npm run build` succeeds. Manually verify by running `npm run dev`:
    - Page loads without console errors
    - ThemeToggle button visible and clickable
    - Clicking toggle switches between dark/light themes
    - Theme persists after page refresh (check localStorage for "theme" key)
    - No flash of wrong theme on reload
    - Smooth color transitions when toggling
  </verify>
  <done>
    Theme system fully working: ThemeProvider wraps app, ThemeToggle switches themes, CSS variables define both palettes, transitions are smooth, preference persists in localStorage, no FOUC, page has placeholder sections with IDs for smooth scroll targets.
  </done>
</task>

</tasks>

<verification>
- `npm run build` produces static output in `out/` directory
- Theme toggle works and persists across refresh
- No hydration warnings in browser console
- `@custom-variant dark` present in globals.css (not JS config)
- `suppressHydrationWarning` on html tag
- Placeholder sections exist with proper IDs for future navigation
</verification>

<success_criteria>
- FOUND-03: Dark and light themes with smooth transitions (CSS transition on bg/color/border)
- FOUND-04: Theme toggle component present (ThemeToggle.tsx)
- FOUND-05: Theme persists via next-themes localStorage
- FOUND-06: Static export builds successfully (baseline for CWV)
- FOUND-08: Next.js 16 with SSG via output: 'export'
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-setup/01-01-SUMMARY.md`
</output>
